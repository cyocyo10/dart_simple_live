# 变更记录

## 1.14.1+11402 — 待发布

- 桌面直播画面支持按住鼠标左键上下拖动调音量，覆盖普通、全屏和小窗，灵敏度按播放器高度计算。
- 单击音量按钮打开滑块面板，显示百分比并提供静音/恢复；支持点击外部或 Esc 关闭。
- 连续音量调整合并为最新值，避免播放器响应较慢时积压大量设置请求；滑块拖动结束再保存默认音量。
- 主窗口与直播窗口先显示加载反馈，诊断日志记录各启动阶段耗时；子窗口不再重复定时刷新整份关注列表和账号资料。
- 共享存储轮询先检查文件元数据，空闲时减少完整快照读取；保留定期强制校验及跨窗口更新。
- 观看记录使用打开房间的时刻排序，慢请求、重新连接及旧窗口不会把记录错误置顶。
- 历史页在常用宽窗口恢复双列、窄窗口单列，按行排列；主播、平台状态与本地观看时间分层显示，兼容大字号。
- 桌面字体、菜单、弹窗和按钮提示统一样式，保留自选主题色；移动端保持原交互。

## 1.14.0+11401 — 待发布

此版本为 cyocyo10 维护的 fork；CI 构建包不代表已经发布正式 Release。

- 迁入 AllLive 桌面体验：独立直播窗口、共享收藏与历史、弹幕字体/字号/字重设置。
- 修复抖音登录入口返回 4031，补充官网登录框定位和扫码后的登录态检测。
- 改善播放中关闭窗口的退出流程，增加直播画面鼠标滚轮调音量。
- 菜单打开期间保留原选项快照，后台刷新不会把旧选择套到新线路/画质。
- Windows 清晰度/线路使用鼠标菜单，设置使用有边界的弹窗；窄窗口控制栏可拖动和滚动，手机保留原交互。
- Windows 便携包采用单入口与 runtime 目录，文件名标明版本和构建号；完善诊断日志。
- Windows 便携版与 MSIX 安装包拆为独立构建任务和下载项，一种打包失败不影响另一种。
- GitHub Actions 升级 Node 24，Android runner 固定 Ubuntu 24.04。
- 统一应用版本与版本元数据；标签构建使用标签提交，并在构建前核对版本。

Windows 真实账号登录、不同 DPI 下的显示与退出手感需人工验收；自动检查不代替这些验证。
