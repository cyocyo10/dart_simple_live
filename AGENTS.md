# Dart Simple Live — 项目协作约定

本文件面向维护者使用 Astra 的主会话，并补充用户全局 AGENTS.md。模型由用户或客户端选择；本文件不切换模型、不授予工具权限，也不代替实际隔离。用户当前明确要求优先。

## 目标与完成边界

- 本仓库是 `cyocyo10/dart_simple_live`，独立制定路线与版本。主要目标是把 AllLive 的 Windows 使用体验维护在 Dart 客户端中，兼顾 Android；保留其他客户端源码，但不能据此宣称它们已经验收。
- 按任务需要阅读代码与文档。迁移行为查 `docs/ALLLIVE_MIGRATION.md`；分支、上游及发布查 `docs/FORK_MAINTENANCE.md`；本地验证查 `CONTRIBUTING.md`；历史证据查 `docs/worklog/`。小修改不要求通读这些文件。
- 在当前授权范围内持续完成实现、相关验证、问题修复和交付；若已授权 commit、push、监控 CI，就完成这些步骤，不在“写完代码”时提前停止。同一授权不重复确认。
- 区分已实现、自动验证和真人验收。无法在 Linux 直接验证的 Windows 账号登录、DPI、窗口手感应如实说明，不能用 CI 成功替代。

## Astra 主会话与执行分工

- 主会话负责关键设计、复杂歧义、集成和最终验收。Astra 仅用于用户当前主会话；没有用户明确授权“额外启动 Astra 子代理”，不得创建 Astra 子代理或通过其他进程绕过。
- 只使用已配置的角色名：`explorer`（Luna High，只读探索）、`quick_editor`（Luna High，清晰局部低风险修改）、`sol_implementer`（Sol Medium，常规实现）、`sol_complex`（Sol High，跨模块/业务语义/并发等复杂实现）。不以显式模型参数绕过角色路由。
- 最多同时 3 个子代理；只委派输入明确、可独立验收且收益超过交接成本的任务。执行子代理不得继续委派或启动其他编码代理；需要升级时交回主会话。
- 同一工作目录只能有一个写入者。并行写入须使用独立 worktree，逐个核实基线、实际路径、项目/局部指令和配置；被忽略但必需的规范按明确清单加载，不复制凭据或整个工作目录。无法确认时退回单写入者。
- 交接给出所有权、目标、范围、约束、基线和验收证据，并提醒不撤销他人改动。验证前检查共享服务、端口、缓存和生成目录的冲突；最终由唯一集成人验收完整结果，不重复已经获得充分证据的检查。
- 记录可观察的运行模型、effort、用量和验收问题；未知项写未知。配置和行为指引不等于运行时证据或硬性锁定。

## 项目约束

- `simple_live_core` 负责平台协议与弹幕；`simple_live_app` 负责客户端、账号和持久化；本地插件补丁保留来源与说明。不要把网络/存储逻辑堆到页面中。
- 多窗口各自管理播放器、音量、全屏与关闭；收藏、历史、设置和账号走已有共享存储。保留跨进程锁、原子写入、合并/删除语义和迁移备份，禁止退回多个进程直接写同一 Hive 文件。
- 历史按打开直播间的时刻记录，慢响应和旧窗口不得回退或抢占顺序。关闭流程需处理迟到回调、超时和重复调用；子窗口不重复运行主窗口的全量后台刷新。
- Windows 的鼠标、键盘、窗口布局与移动端触屏分别处理；更改弹幕样式应落实到渲染参数和缓存失效。保留普通/小窗/全屏及窄窗口、大字号的兼容性。
- 日志和诊断导出不得包含 Cookie、token、密码或完整个人数据。损坏恢复、导入覆盖、清空与迁移不得静默丢数据。`.local-archive/`、签名文件、账号文件、私有配置和原始敏感日志不进 Git。

## 分支、版本与发布

- `origin` 是自己的 fork，`upstream` 只用于读取原作者更新。操作 GitHub 前核对仓库身份，显式指定本仓库（多数子命令用 `-R cyocyo10/dart_simple_live`，repo 子命令直接传仓库名）；本机配置的 `git gh` 会使用仓库代理。
- 日常工作落在 `dev` 或短期分支；验收后以 fast-forward 将 `dev` 推进到 `master`。`master` 是默认分支。不要常态化重写共享历史，也不要把旧 master 的历史重新合回已整理的 dev。
- 上游修复在 `sync/upstream-*` 中检查后按需 `cherry-pick -x`；整批 merge 应有明确需要和差异审查。保留原作者提交、LICENSE 和出处，不自动同步、不向上游推送或发送消息。
- 应用版本由 `python3 tool/app_version.py --set X.Y.Z+BUILD` 同步；实际产品变更递增构建号并更新 `CHANGELOG.md`。仅文档/CI/维护规则变更可保持应用版本。TV/core 版本独立。
- 普通分支构建产出 Actions artifacts。`vX.Y.Z` 对应 master，`dev_vX.Y.Z` 对应 dev；标签必须匹配源码版本。标签流水线成功后生成 Release 草稿，公开发布按维护者的明确发布指令处理。
- 推送已经获授权时，跟进对应 SHA 的 CI；失败根据日志修复并重验受影响步骤。清理构建历史前保留当前可用产物及必要证据，不把失败记录删掉当作修复。

## Code Review Rules

- 优先检查数据丢失、跨窗口竞态、播放/关闭生命周期和登录凭据泄漏；风格问题交给现有格式化与分析工具。
- 针对改变的行为选择有效验证；存储修改必须覆盖真实多进程，播放器修改覆盖迟到请求/销毁，桌面交互覆盖对应输入与布局。仅说明文字或简单可逆修改无需机械新增测试。
- 交付说明变化、验证结果和实际限制；必要证据整理到 `docs/worklog/`，大日志与私有备份留在本地归档。避免将历史总结写成永不过时的现状。
